{% for article in articles %}
    <div class="card mb-2">
        <div class="card-body">
            <h4 class="card-title">{{ article['titre'] }}</h4>
            <p class="card-text">{{ article['resume'] }}</p>
        </div>
    </div>
{% endfor %}