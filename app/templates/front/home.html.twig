{% extends "base.html.twig" %}

{%  block title %}Page de bienvenue{% endblock %}

{% block body %}
<h2>{{ titre }}</h2>
    <div class="row">
        <div class="col-3">
            {{ include("front/_menu.html.twig", {currentPage: 0}) }}
        </div>
        <div class="col-9">
            <!-- insertion du rendu d'une méthode de controller -->
            {{ render( path('articles') ) }}
{#            {{ render( controller("App\\Controller\\HomeController::listeArticle") ) }}#}
        </div>
    </div>
{%  endblock %}